[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "arm3dof"
version = "0.1.0"
description = "Analytic kinematics and calibration tools for a 3-DOF robotic arm"
requires-python = ">=3.10"
authors = [{name = "Vivek Vala"}]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
